// import { getPosts } from "../API/user_api";

// // getPosts().then(data => console.log(data));

const str = new Date().getTime().toLocaleString('en-US', { timeZone: 'Asia/Jakarta' });
console.log(str);